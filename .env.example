# --- YouTube Data API ---
# An API key from the Google Cloud Console with the YouTube Data API v3 enabled.
# Used for fetching video metadata like titles, descriptions, and durations.
YOUTUBE_API_KEY="your_youtube_api_key_here"

# --- Source Database (MySQL) ---
# Connection details for the source MySQL database containing the initial video list.
MYSQL_HOST="localhost"
MYSQL_USER="your_mysql_username"
MYSQL_PASSWORD="your_mysql_password"
MYSQL_DATABASE="your_mysql_database_name"

# --- Local Cache Database (SQLite) ---
# The name of the local SQLite database file that will be created to cache
# the data from MySQL. This avoids repeated calls to the source database.
SQLITE_DB_NAME="local.sqlite"

# --- Vector Database (PostgreSQL with PGVector) ---
# The full connection string for the PostgreSQL database where the final
# vector embeddings will be stored and queried.
# Format: postgresql+psycopg2://<user>:<password>@<host>:<port>/<database>
POSTGRES_DB_PATH="postgresql+psycopg2://postgres:your_postgres_password@localhost:5432/kfai_db"
